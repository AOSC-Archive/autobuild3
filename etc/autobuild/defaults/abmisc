# Autobuild Manifests

# MAKEFLAGS usage. ABMK as supplement.
if [[ $NOPARALLEL = yes ]]; then
    MAKEFLAGS=
else
    MAKEFLAGS="-j$(echo $(lscpu | grep ^CPU\(s\) | awk '{ print $2 }')*2+1 | bc)"
fi

ABARCHIVE=autobuild-aoscarchive
ABINSTALL=yes
ABSHADOW=yes

# LTO section
NOLTO=yes

# Clang or not?
USECLANG=no

# Want those big fat static libraries?
NOSTATIC=yes
