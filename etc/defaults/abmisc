# Autobuild Manifests
if [ "x$ABMK" = "x" ]; then
    ABMK="-j$(echo $(lscpu | grep ^CPU\(s\) | awk '{ print $2 }')*2+1 | bc) V=1"
else
    true
fi
ABARCHIVE=autobuild-aoscarchive
ABINSTALL=yes
ABSHADOW=yes

# LTO section
NOLTO=yes
